function Logo({text}) {
  return (
    <div className="">
      <h1 className={`${text} text-[#DF826C] font-bold`}>
        Shofor<span className="  ">Pothe</span>
      </h1>
    </div>
  );
}

export default Logo;
