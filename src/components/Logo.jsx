function Logo({text}) {
  return (
    <div className="">
      <h1 className={`${text} text-white  font-bold`}>
        shofor<span className=" text-red-500 animate-pulse">Pothe</span>
      </h1>
    </div>
  );
}

export default Logo;
